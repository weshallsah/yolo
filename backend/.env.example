APP_DETECTION_CONFIDENCE_THRESHOLD=0.2

# Which head the weights were trained with: 'detect' (boxes) or 'classify' (whole-image
# labels). A classify checkpoint served as 'detect' returns no results at all.
# APP_MODEL_TASK=classify
# APP_YOLO_WEIGHTS_PATH=models/retail_yolo_products_cls/weights/best.pt
